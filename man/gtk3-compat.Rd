% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat.R
\name{gtk3-compat}
\alias{gtk3-compat}
\alias{gTimeoutAdd}
\alias{gIdleAdd}
\alias{gtkDialogRun}
\alias{gtkFileChooserDialogRun}
\title{GTK3-Style Compatibility Helpers}
\usage{
gTimeoutAdd(interval, fun)

gIdleAdd(fun)

gtkDialogRun(dialog)

gtkFileChooserDialogRun(parent = NULL, title = "Choose File", action = 0L)
}
\arguments{
\item{interval}{Interval in milliseconds between calls}

\item{fun}{R function to call. Should return TRUE to continue, FALSE to stop.}

\item{dialog}{GtkDialog external pointer}

\item{parent}{Parent window (external pointer) or NULL}

\item{title}{Dialog title string}

\item{action}{File chooser action: 0 = open file, 1 = save file, 2 = select folder}
}
\value{
For \code{gTimeoutAdd} and \code{gIdleAdd}: source ID (invisibly) that
  can be passed to \code{gSourceRemove()} to cancel the callback.

For \code{gtkDialogRun}: integer response code (GTK_RESPONSE_ACCEPT,
  GTK_RESPONSE_CANCEL, etc.)

For \code{gtkFileChooserDialogRun}: list with \code{response} (integer)
  and \code{file} (selected file path or NULL if cancelled)
}
\description{
GTK3-style blocking APIs that are simpler alternatives to GTK4's async patterns.
These functions provide familiar interfaces for users migrating from GTK3/RGtk2.
}
